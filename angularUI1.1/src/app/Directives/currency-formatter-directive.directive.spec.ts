import { CurrencyFormatterDirectiveDirective } from 'app/CurrencyFormatter/currency-formatter-directive.directive';

describe('CurrencyFormatterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyFormatterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
